# Brief: build Pueth, and move the goblin camp there

Start from the scaled world (`docs/world-scale-report.md`: one authored hex is 100 m; hand-placed coordinates are written as authored numbers and pass through `src/world-scale.js`; every place is a rigid cluster). Read `docs/original-brief.md` (search for "Pueth") and `docs/brief-review.md` first: the user's brief gives Drent four neighbours, and Pueth is the one to the north. Luscia must have no goblins (wolves are its danger), so the Bramble Scout Camp leaves Luscia for the woods of southern Pueth, where the brief says bramble goblins live and from where they have been spilling into Drent.

## The region, true to the map

- Atlas facts: Pueth has 27 hexes (15 grassland, 7 hills, 5 plains) and lies north of Drent, with Feradom beyond it and the northern sea on its coast. Lore: `../world-builder/azhora_lore/geography/regions/pueth.md` (cold, peripheral timber country; the Feradom barrier hills dying away northward; cold-birch logged for Ambron's lake shipwrights; timber roads; Drentish-speaking people who meet outside officials with measured patience; rivers draining south toward Drent and north to the coast). `src/campaign-world.js` has it at level 1: the Empire holds the west and the main town on the road to Feradom, rebels hold the east, bramble goblins in the wooded south, hill goblins in the hillier north.
- **Use the map, including its rivers.** The user was explicit: consider the map and things like rivers, and a river runs along part of the Drent–Pueth border. River edges are authored in `../world-builder/map/resources/examples/azhora.wwmap` (`map.rivers`, keyed by hex edge; `scripts/export-world-map.mjs` shows how to read and chain them). See how the Caloss was brought into the playable world and do the same for the border river and any other river inside Pueth or on its borders: real water, banks, the shared water metadata the minimap and trail charts read, fishing only if it is cheap. Do not invent rivers the map does not have, and do not leave out ones it has. Give the border river a name in the Drentish style and record it in the report.
- Add `'Pueth'` to `PLAYABLE_REGIONS` with its own biome in `REGION_BIOMES`: mixed cold woodland in the south (birch and conifer among Drent's broadleaf near the border, thinning northward), open valley grassland, and bare-shouldered hills in the north. It must read as colder and barer than Drent at a glance. Regenerate `src/region-survey.js` (`node scripts/build-region-survey.mjs`). Region membership, outlines, minimap, trail charts, the chart marker, the region card and autosave on entry, `WORLD_BOUNDS`, the developer atlas's local destinations and the region tests all follow. **Another agent is adding West Suval at the same time**: write code and tests that derive from `PLAYABLE_REGIONS` and never from a count, keep your edits to shared registries small, and expect `src/region-survey.js` to be regenerated once more after both branches merge.
- The way in: a road north out of Drent from the Greenway or the village's northern trail (look at what exists: `world.northTrail`, the woodland paths; the Tidehaven cluster is rigid and must come out unchanged, so join the new road at its edge), through Drent's forest to the border river, across it by a **timber bridge** (or a ford if the map's river is small there), and on through southern Pueth to the main town. Signposts at the junction and the bridge in the existing sign style (another agent is unifying signs; use the current helpers and it will restyle them).
- Build the south well and the rest honestly: the southern third (the border river, the woods, the Legion's road post, the goblin country, the road) gets real places and detail; the main town on the Feradom road gets a modest first version (palisaded timber town, 8 to 12 buildings, a Legion garrison house, a timber yard stacked with cold-birch, four to six townsfolk with ambient lines in the plain local voice); the hills, the east (rebel country) and the coast get terrain, scatter, a few landmarks with discovery text and nothing else. No rebel quest, no hill-goblin quest yet: leave the east as rumour in dialogue.

## The goblin camp moves here

- Today: `HIDEOUT_SITE`/`hideoutToWorld` in `src/region-world.js` put the camp in north Luscia (authored centre (-456, 154), trail from the main road at (-397, 153)); `src/forest-hideout.js` is the quest (approach, supplies, encounter, reward 30 copper, `minimumQuestStage: 10`); the garrison that tells of it and marches with the traveler (`HIDEOUT_GARRISON`: Captain Decimus Varo, Legionary Casso, Legionary Brill; the `escort` flag and the follow logic in `src/main.js`) stands in Lumber Town at `garrison-*` stands in `src/region-world.js`; `src/world-scale.js` has a `goblin-camp` cluster for it; `src/forest-hideout-smoke.js` is marked stale.
- Move all of it: the camp to a site in the woods of southern Pueth, 150 to 300 m off the new road along a trail marked with the same scraps of blue cloth, chosen like the last site was (level enough for the camp frame, the encounter valid under `combat.startEncounter`'s rules with the retreat toward the road on a +axis, clear of other places). The garrison to a **Legion road post at the Pueth end of the border bridge** (a watch hut, a palisaded yard, a beacon, the three soldiers; Legion armor, men, speaking in orders). Casso tells of the camp, the Captain offers to go, and they march with the traveler exactly as now. Remove the camp, its trail, its cluster and the garrison stands from Luscia and leave no trace in Lumber Town's dialogue; say in the report what you did with anything in `src/luscia-town.js` or `src/luscia-chapter.js` that mentioned them.
- Story sense: the garrison should connect the camp to the raid on Tidehaven at the start of the game (these are the goblins who have been crossing the river into Drent), and Tidehaven people may now point north when asked about the raid (one or two lines, no new quest stages in the tutorial). The quest stays optional and must never block the main road south-west. Decide and justify when it becomes available (the old rule was after the road out of Drent was filed, `questStage >= 10`; an earlier opening is reasonable now that the way north starts near the village, but Drent is level 0, so nothing may attack the player south of the river outside a quest).
- Old saves: a checkpoint with the camp accepted, scouted or cleared in Luscia must load and keep its state (cleared stays cleared and paid; an accepted but unfinished quest points at the new site). A saved position inside the old camp ground is simply a position in Luscia's woods.
- Rewrite `src/forest-hideout-smoke.js` for the new flow, run it once, and fix what it finds.

## Rules

- Work only in your own worktree and branch. Two other agents are working in parallel: one builds West Suval and Solis (new region, border chapter), one fleshes out the existing towns, unifies the signs and closes the East Suval border. The lead is building riding in `src/main.js`. Put Pueth's scenery, places and people in new modules with a single call from the world builder and one hook in `src/main.js` (CRLF, very long lines: never reformat, patch by exact anchors), and keep edits to shared files small and local.
- Only Legion people wear Legion armor; soldiers are men by default; locals speak plainly; there is no South Pyros; do not use the novella's events. Tone rules: `docs/content-pass.md`.
- Performance: reuse materials, instancing and the per-block scatter batching; terrain tiles are added, vertex spacing kept. The traversal smoke's frame times should not get worse by more than about 15% in the existing regions.
- `npm test` stays green; add tests for the region (membership, outline, the river where the map has it and nowhere else, the bridge walkable end to end, the road clear of colliders), for every new stand (standable, reachable, 4 m from quest people), for the encounter's validity, and for old-save loading. Run `npm run test:game`, `npm run test:hideout` and `npm run test:road` once each at the end and fix what they find; do not loop on smokes. Check your work by eye with review screenshots of the bridge, the post, the camp and the town.
- `npm run test:autoplay` is known not to be green at the new scale (it reaches the outpost's gate; see `docs/world-scale-report.md`) and takes up to half an hour. Do not run it; keep the autopilot's unit tests green and say in your report what a rendered autoplay run would need.
- Commit in milestones; write `docs/pueth-report.md` (what the map gave you and how you used it, the river's course, site coordinates authored and world, files touched, smoke results, what is left as a stub).
